pre-commit:
  parallel: true
  jobs:
    - name: typecheck
      run: pnpm exec tsc --noEmit

    - name: build
      run: pnpm build

    - name: test
      run: pnpm test

    - name: spell
      glob: "*.{ts,md}"
      run: pnpm exec cspell --no-progress {staged_files}
